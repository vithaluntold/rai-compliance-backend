services:
  - type: web
    name: rai-compliance-backend
    env: python
    plan: starter
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn minimal:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/v1/health