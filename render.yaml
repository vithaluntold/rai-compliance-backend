services:
  - type: web
    name: rai-compliance-backend
    env: python
    plan: starter
    buildCommand: echo "No build needed"
    startCommand: python ultra-minimal.py
    healthCheckPath: /api/v1/health